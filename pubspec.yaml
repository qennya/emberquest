name: emberquest
description: Emberquest HW 3 game
version: 0.0.0
publish_to: none

environment:
  sdk: ^3.0.0
  flutter: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.33.0

dev_dependencies:
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true  # optional, but common
  assets:
    - assets/images/
